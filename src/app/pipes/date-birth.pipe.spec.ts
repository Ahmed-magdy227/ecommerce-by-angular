import { DateBirthPipe } from './date-birth.pipe';

describe('DateBirthPipe', () => {
  it('create an instance', () => {
    const pipe = new DateBirthPipe();
    expect(pipe).toBeTruthy();
  });
});
