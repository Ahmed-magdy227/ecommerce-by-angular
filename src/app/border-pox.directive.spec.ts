import { BorderPoxDirective } from './border-pox.directive';

describe('BorderPoxDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderPoxDirective();
    expect(directive).toBeTruthy();
  });
});
